
# Application
bind = $mainMod, E, exec, wlr-which-key -k e

# Clipboard
bind = $mainMod, V, exec, foot --title clipse_clipboard -e clipse

# Voice HUD: hl.bind SUPER+A launches the overlay (settings/keybindings.lua)

